{
  "devDependencies": {
    "prettier": "^2.6.2"
  },
  "private": true,
  "scripts": {
    "start": "http-server -c-1 ./pages",
    "lint": "prettier --ignore-path .gitignore --check ./pages"
  },
  "dependencies": {
    "http-server": "^14.1.0"
  }
}